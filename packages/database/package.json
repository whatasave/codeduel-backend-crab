{
  "name": "@codeduel-backend-crab/database",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/pg": "^8.11.11",
    "kysely-codegen": "^0.18.2",
    "kysely-ctl": "^0.12.1"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "scripts": {
    "generate": "kysely-codegen --out-file ./src/database.ts",
    "reset": "kysely migrate:rollback --all && kysely migrate:latest && bun generate"
  },
  "dependencies": {
    "kysely": "^0.27.6",
    "pg": "^8.13.3"
  }
}
